# Implementation Plan: Embedding Pipeline

**Branch**: `003-embedding-pipeline` | **Date**: 2026-03-05 | **Spec**: [/Users/nd/Developer/job/specs/003-embedding-pipeline/spec.md](/Users/nd/Developer/job/specs/003-embedding-pipeline/spec.md)
**Input**: Feature specification from `/Users/nd/Developer/job/specs/003-embedding-pipeline/spec.md`

## Summary

US1 and US2 are already implemented and test-stable (embedding generation reliability + active target identity isolation). The remaining planning focus is US3: make embedding refresh explicitly snapshot-aligned, remove fingerprint-driven refresh semantics for this feature scope, and ship idempotent orchestration plus test closure.

## Technical Context

**Language/Version**: Python 3.12  
**Primary Dependencies**: FastAPI, SQLModel/SQLAlchemy, asyncpg, pgvector, LiteLLM, Pydantic  
**Storage**: PostgreSQL (`job`, `job_embedding`, `sync_run`)  
**Testing**: pytest  
**Target Platform**: Linux container runtime (local + Cloud Run style deployment)  
**Project Type**: Backend web service + batch/scheduled enrichment pipeline  
**Performance Goals**: Bounded provider retries; idempotent upsert; avoid duplicate embedding writes across repeated snapshots  
**Constraints**: Snapshot-driven refresh semantics for this feature; explicit active target isolation (`kind/revision/model/dim`); deterministic failure behavior  
**Scale/Scope**: Source snapshots across tens to hundreds of thousands of jobs; refresh should operate safely under repeated source sync runs

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-check after Phase 1 design.*

- `.specify/memory/constitution.md` is currently an uninstantiated template with placeholder tokens, not enforceable project principles.
- Result: **PASS (No enforceable constitution gates detected)**.
- Follow-up: once constitution is materialized, re-run gate checks for this plan.

## Project Structure

### Documentation (this feature)

```text
/Users/nd/Developer/job/specs/003-embedding-pipeline/
├── plan.md
├── research.md
├── data-model.md
├── quickstart.md
├── contracts/
│   └── snapshot-embedding-refresh-contract.md
└── tasks.md                # to be generated by /speckit.tasks
```

### Source Code (repository root)

```text
/Users/nd/Developer/job/app/
├── services/
│   ├── infra/embedding/
│   ├── application/full_snapshot_sync/
│   └── application/match_service/
├── repositories/
│   ├── job.py
│   └── job_embedding.py
└── models/
    ├── job.py
    ├── job_embedding.py
    └── sync_run.py

/Users/nd/Developer/job/scripts/
└── run_scheduled_ingests.py

/Users/nd/Developer/job/tests/
├── unit/
│   ├── test_embedding_service.py
│   ├── test_job_embedding_repository.py
│   ├── test_match_query.py
│   ├── test_match_service.py
│   └── sync/
└── integration/
```

**Structure Decision**: Keep current monorepo backend structure. Implement US3 by extending existing application/repository paths rather than introducing new subsystems.

## Phase 0: Research Outcomes

Research artifact: [/Users/nd/Developer/job/specs/003-embedding-pipeline/research.md](/Users/nd/Developer/job/specs/003-embedding-pipeline/research.md)

Resolved questions:
- What is already complete: US1/US2 complete with passing tests.
- What is incomplete: US3 orchestration and semantics alignment.
- What to change next: use snapshot outcome as refresh driver and keep upsert idempotency guarantees.

## Phase 1: Design Outcomes

Design artifacts:
- Data model: [/Users/nd/Developer/job/specs/003-embedding-pipeline/data-model.md](/Users/nd/Developer/job/specs/003-embedding-pipeline/data-model.md)
- Contract: [/Users/nd/Developer/job/specs/003-embedding-pipeline/contracts/snapshot-embedding-refresh-contract.md](/Users/nd/Developer/job/specs/003-embedding-pipeline/contracts/snapshot-embedding-refresh-contract.md)
- Quickstart: [/Users/nd/Developer/job/specs/003-embedding-pipeline/quickstart.md](/Users/nd/Developer/job/specs/003-embedding-pipeline/quickstart.md)

Key design decisions:
- Snapshot success is the orchestration trigger for embedding refresh.
- Refresh scope is lifecycle-aligned (open/eligible jobs from successful snapshot outcomes).
- Writes remain idempotent via active-target uniqueness + upsert behavior.

## Phase 2: Task Planning Approach

Planned task clusters for `/speckit.tasks`:
1. Application orchestration: wire snapshot-success -> embedding refresh flow.
2. Repository semantics: align candidate selection with snapshot-driven refresh policy for this feature scope.
3. Test coverage: add/adjust unit and sync tests for snapshot-triggered refresh and idempotent reruns.
4. Regression validation: run embedding + sync focused suites.

## Complexity Tracking

No constitution-gate violations requiring exception tracking.
